# Goal

**Status**: paused
**Policy**: goal
**Auto-continue**: on
**File**: `20260831093912-qyrek5.md`
**Pause reason**: auditor retry: pi launch failed: spawn pi ENOENT
**Agent suggests**: Auto-retry in 120m — or /goal resume to retry now
**Completion audit**: quota-waiting
**Completion audit attempt**: `audit-mth5c6tl-vhvgl0`

## Objective

> 로드맵 1~3단계 구현: `Communication.Shared` 전체 계약·구현(Sessions/Messages/Channels/Framing/Connection/Threading) + `Communication.Network.TCP` 패키지 + 단위 테스트 + `Sandbox/` 채팅 샘플(서버+클라). 확정된 런타임 계약(끊김 후 송신은 faulted Task, 백프레셔 비동기 대기, InlineDispatch 기본 false, 핸들러 예외는 Trace 후 계속, SendOptions 마커 클래스)을 그대로 구현한다. 직렬화는 Sandbox 로컬 테스트용 Converter만 사용 — 경계: RUDP·TCP_IOCP·IPC 미구현, 라이브러리 재접속·하트비트 없음, DS_MessageProtocol 미참조, `Legacy/` 수정 금지. 부수: `Communication.Shared.csproj` 깨진 Description 수정.

## Verification contract

1. `dotnet build Communication.sln` 에러 0건.
2. `Document/02-Architecture/Code-Structure.md` 목표 트리와 일치: `Source/Communication.Shared/{Sessions,Messages,Channels,Framing,Connection,Threading}`과 `Source/Communication.Network.TCP/`에 구현 .cs 파일 존재(빈 폴더 없음).
3. `Test/`에 테스트 프로젝트 존재, `dotnet test` 전부 통과 — 커버리지: length-prefix 프레이밍, 백프레셔 대기, 끊김 플래그, 끊김 후 송신 faulted Task, 핸들러 예외 격리(수신 루프 생존).
4. `Sandbox/`에 서버+클라이언트 채팅 샘플 존재·빌드 성공, 실행 방법이 문서화되어 있고 로컬 실행으로 연결·메시지 교환·끊김 확인 가능.
5. Document vault 동기화: Feature-Spec F1~F3 구현 상태 반영, Public-API·Code-Structure가 실제 코드와 정합, `_meta/Changelog.md` 항목 추가.
6. `git status` 기준 `Legacy/` 하위 변경 0건.
7. `Source/Communication.Shared/Communication.Shared.csproj`의 `Description`이 깨짐(`???`) 없이 정상 문자열.

## Tasks

- [x] 환경·스캐폴드 확인 (dotnet SDK, sln, 기존 Sandbox 구성) `1`
- [x] Shared 기초 계약: Channels·Connection·Framing·Threading `2`
- [x] Shared 메시지 계층: Converter·Handler·QueueOptions·MessagePipeline `3`
- [x] Shared Sessions (ISession·Session) + csproj Description 수정 `4`
- [x] Test 프로젝트 스캐폴드 (xUnit) + sln 등록 `5`
- [x] Shared 단위 테스트: 프레이밍·백프레셔·끊김 플래그·끊김 후 송신·핸들러 예외 격리 `6`
- [x] Communication.Network.TCP 패키지 (Channel·Connector·Listener·Session·keep-alive) `7`
- [x] TCP loopback 통합 테스트 `8`
- [x] Sandbox Chat.TCP (서버+클라+로컬 Converter) 실행 확인 `9`
- [x] Document vault 동기화 + Legacy 변경 없음 확인 `10`
